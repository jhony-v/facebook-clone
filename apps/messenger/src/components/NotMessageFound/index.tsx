import { Flex } from '@chakra-ui/react'

const NotMessageFound = () => {
  return <Flex flex={1} alignItems="center" justifyContent={"center"}>No messages found.</Flex>
}

export default NotMessageFound